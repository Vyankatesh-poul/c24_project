class Ground {
  constructor(x, y, width, height) {
    var options ={
      isStatic : true
    }
  };
  

  display(){
    rectMode(CENTER);
    fill("brown");
    strokeWeight(4);
  }
}
